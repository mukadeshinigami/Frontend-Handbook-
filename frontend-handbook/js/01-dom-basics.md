# Взаимодействие со страницей (DOM)

## Введение

**DOM (Document Object Model)** — это представление HTML-документа в виде дерева объектов, которое JavaScript может читать и изменять. DOM — это мост между вашим HTML и JavaScript кодом.

Когда браузер загружает HTML-страницу, он создаёт **объектную модель документа** — древовидную структуру, где каждый HTML-элемент становится объектом JavaScript.

---

## Что такое DOM-дерево?

Представьте простой HTML:

```html
<!DOCTYPE html>
<html>
  <head>
    <title>Моя страница</title>
  </head>
  <body>
    <h1>Привет</h1>
    <p>Это абзац</p>
  </body>
</html>
```

Браузер преобразует его в дерево:

```
document
  └── html
      ├── head
      │   └── title
      │       └── "Моя страница"
      └── body
          ├── h1
          │   └── "Привет"
          └── p
              └── "Это абзац"
```

Каждый узел (элемент, текст) — это JavaScript-объект, с которым можно работать.

---

## Поиск элементов на странице

Прежде чем изменить элемент, его нужно найти.

### 1. `document.getElementById('id')`

Находит **один элемент** по уникальному ID:

```javascript
// HTML: <h1 id="title">Заголовок</h1>
const title = document.getElementById('title');
console.log(title); // <h1 id="title">Заголовок</h1>
```

**Особенности:**
- Возвращает **один элемент** или `null`, если не найден
- ID должен быть **уникальным** на странице
- Самый быстрый метод поиска

---

### 2. `document.querySelector('.class')`

Находит **ПЕРВЫЙ** элемент, подходящий под CSS-селектор:

```javascript
// HTML: <p class="intro">Первый</p> <p class="intro">Второй</p>
const firstIntro = document.querySelector('.intro');
console.log(firstIntro); // <p class="intro">Первый</p>
```

**Можно использовать любые CSS-селекторы:**

```javascript
// По классу
document.querySelector('.my-class');

// По тегу
document.querySelector('p');

// По ID (хотя getElementById быстрее)
document.querySelector('#my-id');

// Комбинации
document.querySelector('div.container > p.intro');
```

---

### 3. `document.querySelectorAll('.class')`

Находит **ВСЕ** элементы, подходящие под селектор (возвращает **NodeList**):

```javascript
// HTML: <p class="text">Один</p> <p class="text">Два</p> <p class="text">Три</p>
const allTexts = document.querySelectorAll('.text');
console.log(allTexts); // NodeList(3) [p.text, p.text, p.text]

// Перебор всех найденных элементов
allTexts.forEach(text => {
  console.log(text.textContent); // "Один", "Два", "Три"
});
```

**Особенности NodeList:**
- Похож на массив, но это **не массив**
- Можно использовать `forEach()`, но не `map()`, `filter()` и т.д.
- Чтобы получить полноценный массив: `Array.from(allTexts)`

---

### Сравнение методов поиска

| Метод | Возвращает | Селектор | Скорость |
|-------|-----------|----------|----------|
| `getElementById('id')` | Элемент или `null` | Только ID | ⚡ Самый быстрый |
| `querySelector('.class')` | Первый элемент или `null` | Любой CSS | 🔥 Быстрый |
| `querySelectorAll('.class')` | NodeList | Любой CSS | 🔥 Быстрый |

---

## Манипуляция контентом

### 1. `element.textContent` — изменить текст

Получает или устанавливает **только текстовое содержимое** элемента (без HTML-тегов):

```javascript
const heading = document.querySelector('h1');

// Прочитать текст
console.log(heading.textContent); // "Старый заголовок"

// Изменить текст
heading.textContent = "Новый заголовок";
```

**Важно:** HTML-теги будут интерпретированы как текст, а не код:

```javascript
heading.textContent = "<strong>Жирный</strong>"; 
// Результат на странице: <strong>Жирный</strong> (как текст!)
```

---

### 2. `element.innerHTML` — изменить HTML-содержимое

Получает или устанавливает **HTML-содержимое** элемента (включая теги):

```javascript
const box = document.querySelector('.box');

// Прочитать HTML
console.log(box.innerHTML); // "<p>Старый контент</p>"

// Изменить HTML
box.innerHTML = "<p>Новый <strong>жирный</strong> контент</p>";
```

**⚠️ Осторожно:**
- `innerHTML` может создать уязвимость **XSS** (Cross-Site Scripting), если вставляете пользовательский ввод
- Для текста **всегда используйте `textContent`** — он безопаснее

---

### 3. `element.value` — работа с формами

Получает или устанавливает значение **полей ввода** (`<input>`, `<textarea>`, `<select>`):

```javascript
// HTML: <input type="text" id="username">
const input = document.getElementById('username');

// Прочитать значение
console.log(input.value); // "" (пусто изначально)

// Установить значение
input.value = "Иван";

// Теперь в поле ввода будет "Иван"
```

**Применение:**
- Получение данных из форм
- Валидация ввода
- Динамическая подстановка значений

---

## Манипуляция стилями

### Прямое изменение через `element.style`

Изменяет **инлайновые стили** элемента:

```javascript
const paragraph = document.querySelector('p');

paragraph.style.color = "red";
paragraph.style.backgroundColor = "#f0f0f0";
paragraph.style.fontSize = "20px";
paragraph.style.padding = "10px";
```

**Особенности:**
- Имена CSS-свойств пишутся в **camelCase**: `background-color` → `backgroundColor`
- Значения — **строки**: `"red"`, `"20px"`
- Создаёт атрибут `style` прямо в HTML: `<p style="color: red; ..."></p>`

---

### ⚠️ Проблемы прямого изменения стилей

```javascript
// ❌ НЕ РЕКОМЕНДУЕТСЯ: смешивание стилей с логикой
element.style.fontSize = "18px";
element.style.color = "blue";
element.style.fontWeight = "bold";
element.style.textDecoration = "underline";
```

**Почему плохо:**
1. Стили размазаны по JavaScript-коду
2. Сложно поддерживать и переиспользовать
3. Сложно переопределить через CSS

---

## Манипуляция классами (лучшая практика ✅)

**Правильный подход:** определить стили в CSS, управлять через **классы**.

### CSS:
```css
.highlight {
  background-color: yellow;
  font-weight: bold;
}

.hidden {
  display: none;
}

.active {
  color: green;
  border: 2px solid green;
}
```

### JavaScript:

#### 1. `element.classList.add('class')` — добавить класс

```javascript
const paragraph = document.querySelector('p');
paragraph.classList.add('highlight');
// <p class="highlight">...</p>
```

---

#### 2. `element.classList.remove('class')` — удалить класс

```javascript
paragraph.classList.remove('highlight');
// <p class="">...</p>
```

---

#### 3. `element.classList.toggle('class')` — переключить класс

Если класса нет — добавит, если есть — удалит:

```javascript
const button = document.querySelector('button');

button.addEventListener('click', () => {
  paragraph.classList.toggle('highlight');
  // Первый клик: добавит .highlight
  // Второй клик: удалит .highlight
  // Третий клик: снова добавит...
});
```

---

#### 4. `element.classList.contains('class')` — проверить наличие класса

```javascript
if (paragraph.classList.contains('highlight')) {
  console.log('Параграф подсвечен!');
}
```

---

### Почему `classList` лучше `style`?

| `element.style` | `element.classList` |
|-----------------|---------------------|
| Инлайновые стили в HTML | Стили в CSS-файле |
| Сложно переиспользовать | Переиспользование через классы |
| Смешивание логики и стилей | Разделение ответственности |
| Высокая специфичность | Легко переопределить |
| ❌ Не рекомендуется | ✅ **Лучшая практика** |

---

## Пример: комплексное использование DOM

```html
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>DOM Пример</title>
  <style>
    .highlight {
      background: yellow;
      padding: 5px;
    }
    .hidden {
      display: none;
    }
  </style>
</head>
<body>
  <h1 id="title">Заголовок</h1>
  <p class="text">Обычный текст</p>
  <button id="btn">Переключить</button>

  <script>
    // Поиск элементов
    const title = document.getElementById('title');
    const paragraph = document.querySelector('.text');
    const button = document.getElementById('btn');

    // Изменить контент
    title.textContent = "Новый заголовок";

    // Добавить класс
    paragraph.classList.add('highlight');

    // Обработчик клика
    button.addEventListener('click', () => {
      paragraph.classList.toggle('hidden');
    });
  </script>
</body>
</html>
```

**Что происходит:**
1. При загрузке: заголовок меняется на "Новый заголовок"
2. Параграф получает жёлтый фон (класс `.highlight`)
3. При клике на кнопку: параграф исчезает/появляется (класс `.hidden`)

---

## Итоги

### Что вы изучили:

✅ **Поиск элементов:**
- `getElementById('id')` — один элемент по ID
- `querySelector('.class')` — первый элемент по селектору
- `querySelectorAll('.class')` — все элементы (NodeList)

✅ **Манипуляция контентом:**
- `textContent` — только текст (безопасно)
- `innerHTML` — HTML-содержимое (осторожно!)
- `value` — значение полей ввода

✅ **Стили:**
- `element.style.property` — прямое изменение (не рекомендуется)
- `classList.add()` / `remove()` / `toggle()` — управление классами (✅ лучшая практика)

---

## Что дальше?

В следующих уроках вы изучите:
- **События** — реакция на действия пользователя (клики, ввод текста)
- **Создание и удаление элементов** — динамическое изменение страницы
- **Делегирование событий** — оптимизация обработчиков
- **Работа с формами** — валидация, отправка данных

---

## Практика

Переходите к практическому уроку: **`01-dom-basics-practice.md`**

Там вы создадите интерактивную страницу с изменением контента, стилей и классов!
