# Rust — Роадмап для изучения

## Текущий прогресс (что пройдено)

_Пока не начато — это начальный роадмап_

---

## Рекомендуемый роадмап (следующие блоки)

### Блок 1: Основы синтаксиса и первая программа
**Файлы:** `01-basics.md` + `01-basics-practice.md`

**Цель:** Написать первую программу, понять базовый синтаксис.

**Темы:**
- Установка Rust (rustup, cargo)
- `cargo new`, `cargo build`, `cargo run`
- Функция `main()`
- `println!` макрос
- Переменные: `let`, `mut`
- Примитивные типы: `i32`, `u32`, `f64`, `bool`, `char`
- Комментарии: `//`, `/* */`, `///`
- Shadowing переменных

**Практика:**
- Hello World с cargo
- Калькулятор простых операций
- Конвертер температуры (Цельсий ↔ Фаренгейт)

---

### Блок 2: Функции и управление потоком
**Файлы:** `02-functions-control-flow.md` + `02-functions-control-flow-practice.md`

**Цель:** Научиться писать функции и использовать условия/циклы.

**Темы:**
- Определение функций: `fn`
- Параметры и возвращаемые значения
- Выражения vs утверждения (expressions vs statements)
- `if`, `else if`, `else`
- `if` как выражение
- Циклы: `loop`, `while`, `for`
- `break`, `continue`
- `match` выражение (базово)
- `return` (явный и неявный)

**Практика:**
- Функция для проверки чётности числа
- Калькулятор с match для операций
- Генератор чисел Фибоначчи
- FizzBuzz

---

### Блок 3: Владение (Ownership) и заимствование (Borrowing)
**Файлы:** `03-ownership.md` + `03-ownership-practice.md`

**Цель:** Понять самую важную концепцию Rust — управление памятью без сборщика мусора.

**Темы:**
- Правила владения (ownership rules)
- Стек vs куча (stack vs heap)
- Move семантика
- Copy trait
- Заимствование: `&T` (immutable reference)
- Изменяемое заимствование: `&mut T`
- Правила заимствования (borrowing rules)
- Dangling references — почему в Rust их нет
- Слайсы: `&[T]`, `&str`

**Практика:**
- Функции с передачей владения
- Функции с заимствованием
- Работа со строковыми слайсами
- Поиск первого слова в строке

---

### Блок 4: Структуры (Structs) и методы
**Файлы:** `04-structs.md` + `04-structs-practice.md`

**Цель:** Научиться создавать собственные типы данных.

**Темы:**
- Определение структур: `struct`
- Создание экземпляров
- Доступ к полям
- Tuple structs
- Unit-like structs
- Методы: `impl` блоки
- Associated functions (конструкторы)
- `self`, `&self`, `&mut self`
- Множественные `impl` блоки

**Практика:**
- Структура `Rectangle` с методами площади и периметра
- Структура `User` с методами валидации
- Структура `Point` с методами расстояния
- Игра с несколькими структурами (RPG character)

---

### Блок 5: Перечисления (Enums) и pattern matching
**Файлы:** `05-enums.md` + `05-enums-practice.md`

**Цель:** Освоить enums и мощь pattern matching.

**Темы:**
- Определение enum
- Варианты с данными
- `Option<T>` — замена null
- `Result<T, E>` — обработка ошибок
- `match` выражение (углублённо)
- `if let` синтаксис
- `while let`
- Паттерны: `_`, `..`, `@`, guards

**Практика:**
- Enum для состояний светофора
- Парсер команд CLI (Command enum)
- Калькулятор с Result для обработки ошибок
- Обработка Option для безопасного доступа

---

### Блок 6: Коллекции (Collections)
**Файлы:** `06-collections.md` + `06-collections-practice.md`

**Цель:** Научиться работать с динамическими структурами данных.

**Темы:**
- `Vec<T>` — динамический массив
- Методы: `push`, `pop`, `len`, `get`, `iter`
- `String` vs `&str`
- Методы строк: `push_str`, `format!`, `chars`, `split`
- `HashMap<K, V>`
- Методы: `insert`, `get`, `entry`, `or_insert`
- Итераторы: базовое знакомство

**Практика:**
- Программа для подсчёта слов в тексте
- Телефонная книга (HashMap)
- Работа со списком покупок (Vec)
- Фильтрация и трансформация данных

---

### Блок 7: Обработка ошибок
**Файлы:** `07-error-handling.md` + `07-error-handling-practice.md`

**Цель:** Научиться правильно обрабатывать ошибки.

**Темы:**
- `panic!` макрос
- `Result<T, E>` (углублённо)
- `unwrap()`, `expect()`
- Оператор `?` для проброса ошибок
- Создание собственных типов ошибок
- `Box<dyn Error>`
- `match` vs `if let` vs `?`
- Best practices обработки ошибок

**Практика:**
- Чтение файла с обработкой ошибок
- Парсер с кастомными ошибками
- Калькулятор с propagation ошибок
- Валидатор с Result

---

### Блок 8: Дженерики (Generics)
**Файлы:** `08-generics.md` + `08-generics-practice.md`

**Цель:** Писать переиспользуемый код для разных типов.

**Темы:**
- Дженерик функции
- Дженерик структуры
- Дженерик enum (Option, Result — как примеры)
- Дженерик методы
- Ограничения типов (trait bounds)
- Множественные параметры типов
- `where` клауза

**Практика:**
- Функция поиска максимума (generic)
- Структура `Point<T>` для разных типов
- Обёртка `Wrapper<T>` с методами
- Generic коллекция (Stack)

---

### Блок 9: Трейты (Traits)
**Файлы:** `09-traits.md` + `09-traits-practice.md`

**Цель:** Понять интерфейсы Rust и полиморфизм.

**Темы:**
- Определение trait
- Реализация trait: `impl Trait for Type`
- Методы по умолчанию
- Trait bounds
- Trait как параметры: `impl Trait`
- Trait как возвращаемые значения
- Trait objects: `dyn Trait`
- Derive макросы: `#[derive(Debug, Clone)]`
- Стандартные трейты: `Display`, `Debug`, `Clone`, `Copy`, `PartialEq`

**Практика:**
- Trait `Drawable` для геометрических фигур
- Trait `Summary` для текстов
- Реализация собственного `Display`
- Коллекция разных типов через trait objects

---

### Блок 10: Тестирование
**Файлы:** `10-testing.md` + `10-testing-practice.md`

**Цель:** Научиться писать тесты.

**Темы:**
- `#[test]` атрибут
- `assert!`, `assert_eq!`, `assert_ne!`
- `#[should_panic]`
- `Result<T, E>` в тестах
- Организация тестов: unit vs integration
- `cargo test`
- Фильтрация тестов
- `#[cfg(test)]` модуль
- Документационные тесты

**Практика:**
- Тесты для математических функций
- Тесты для структур с методами
- Тесты с ожидаемыми ошибками
- Integration тесты для библиотеки

---

### Блок 11: Итераторы и замыкания (Closures)
**Файлы:** `11-iterators-closures.md` + `11-iterators-closures-practice.md`

**Цель:** Писать функциональный код.

**Темы:**
- Замыкания: `|x| x + 1`
- Захват переменных: move, borrow
- Fn, FnMut, FnOnce трейты
- Итераторы: `iter()`, `iter_mut()`, `into_iter()`
- Методы итераторов: `map`, `filter`, `fold`, `collect`
- Ленивость (lazy evaluation)
- Создание собственных итераторов

**Практика:**
- Фильтрация и трансформация коллекций
- Обработка файлов построчно
- Генератор последовательностей
- Цепочки итераторов (method chaining)

---

### Блок 12: Умные указатели (Smart Pointers)
**Файлы:** `12-smart-pointers.md` + `12-smart-pointers-practice.md`

**Цель:** Понять управление памятью через умные указатели.

**Темы:**
- `Box<T>` — выделение в куче
- `Rc<T>` — reference counting
- `Arc<T>` — atomic reference counting
- `RefCell<T>` — interior mutability
- `Mutex<T>` и `RwLock<T>` (кратко)
- Deref trait
- Drop trait
- Комбинации: `Rc<RefCell<T>>`

**Практика:**
- Связный список через Box
- Дерево с несколькими владельцами (Rc)
- Граф через Rc и RefCell
- Разделяемое состояние

---

### Блок 13: Конкурентность (Concurrency)
**Файлы:** `13-concurrency.md` + `13-concurrency-practice.md`

**Цель:** Писать многопоточный код безопасно.

**Темы:**
- Потоки: `std::thread::spawn`
- `join()` handles
- `move` замыкания для потоков
- Передача данных: channels (`mpsc`)
- `send()` и `recv()`
- Разделяемое состояние: `Mutex<T>`, `Arc<Mutex<T>>`
- Deadlocks и как их избегать
- Send и Sync трейты

**Практика:**
- Параллельный подсчёт суммы
- Producer-consumer через channels
- Веб-скрапер с потоками
- Многопоточная обработка файлов

---

### Блок 14: Модули и организация кода
**Файлы:** `14-modules.md` + `14-modules-practice.md`

**Цель:** Организовать большой проект.

**Темы:**
- Модули: `mod`
- Видимость: `pub`
- Вложенные модули
- Файлы как модули
- `mod.rs` vs `название.rs`
- `use` для импорта
- `as` для алиасов
- `pub use` для реэкспорта
- `super`, `self`, `crate`
- Workspaces

**Практика:**
- Разделение проекта на модули
- Библиотека с публичным API
- Workspace с несколькими крейтами
- CLI приложение с модульной структурой

---

### Блок 15: Cargo и зависимости
**Файлы:** `15-cargo.md` + `15-cargo-practice.md`

**Цель:** Научиться управлять проектом и зависимостями.

**Темы:**
- `Cargo.toml` структура
- Зависимости с crates.io
- Версионирование: semver
- Features flags
- `cargo build --release`
- `cargo doc`
- `cargo clippy`
- `cargo fmt`
- Профили сборки
- Публикация крейта (кратко)

**Практика:**
- Проект с внешними зависимостями (serde, reqwest)
- Настройка features
- Оптимизация размера бинарника
- Документирование крейта

---

### Блок 16: Макросы
**Файлы:** `16-macros.md` + `16-macros-practice.md`

**Цель:** Понять метапрограммирование.

**Темы:**
- Декларативные макросы: `macro_rules!`
- Паттерны: `$x:expr`, `$($y:tt)*`
- Повторения
- Hygiene
- Процедурные макросы (введение)
- Derive макросы
- Attribute-like макросы
- Function-like макросы

**Практика:**
- Макрос для упрощённого создания HashMap
- Макрос для логирования
- Derive макрос (если готовы)
- Упрощение повторяющегося кода

---

### Блок 17: Файловый ввод-вывод
**Файлы:** `17-file-io.md` + `17-file-io-practice.md`

**Цель:** Работать с файлами и директориями.

**Темы:**
- `std::fs::File`
- Чтение: `read_to_string`, `BufReader`
- Запись: `write`, `BufWriter`
- Создание/удаление файлов
- Работа с путями: `Path`, `PathBuf`
- Обход директорий: `read_dir`
- Метаданные файлов
- Обработка ошибок при работе с файлами

**Практика:**
- Программа для подсчёта строк в файле
- Копирование файлов
- Поиск файлов по расширению
- Парсер CSV

---

### Блок 18: Сети и HTTP
**Файлы:** `18-networking.md` + `18-networking-practice.md`

**Цель:** Делать HTTP-запросы и создавать серверы.

**Темы:**
- `reqwest` для HTTP клиента
- `tokio` — async runtime (введение)
- `async`/`await` синтаксис
- `hyper` или `axum` для серверов
- JSON: `serde_json`
- REST API клиент
- Простой HTTP сервер

**Практика:**
- HTTP клиент для получения данных с API
- Парсинг JSON ответов
- Простой REST API сервер
- WebSocket клиент (опционально)

---

### Блок 19: Async/Await и асинхронное программирование
**Файлы:** `19-async.md` + `19-async-practice.md`

**Цель:** Писать эффективный асинхронный код.

**Темы:**
- `async fn`
- `Future` trait
- `.await`
- `tokio::spawn`
- `tokio::select!`
- `tokio::join!`
- Async streams
- Cancellation
- Blocking vs async
- Async trait (workarounds)

**Практика:**
- Параллельные HTTP запросы
- Асинхронная обработка файлов
- Чат-сервер с tokio
- Скачивание файлов параллельно

---

### Блок 20: CLI приложения
**Файлы:** `20-cli.md` + `20-cli-practice.md`

**Цель:** Создавать профессиональные CLI инструменты.

**Темы:**
- `clap` для парсинга аргументов
- Subcommands
- Обработка stdin/stdout
- Цветной вывод: `colored`
- Progress bars: `indicatif`
- Конфигурационные файлы
- Обработка сигналов (Ctrl+C)
- Кросс-платформенность

**Практика:**
- CLI калькулятор с аргументами
- Поисковик по файлам (grep-like)
- Генератор файлов проекта
- Утилита для работы с JSON

---

### Блок 21: Базы данных
**Файлы:** `21-databases.md` + `21-databases-practice.md`

**Цель:** Работать с SQL и NoSQL базами.

**Темы:**
- `sqlx` для SQL (PostgreSQL, MySQL, SQLite)
- Compile-time проверка запросов
- Миграции
- Connection pooling
- ORM: `diesel` (кратко)
- Redis клиент
- MongoDB клиент

**Практика:**
- CRUD приложение с SQLite
- Миграции базы данных
- Connection pool в async контексте
- Кеширование через Redis

---

### Блок 22: Веб-фреймворки
**Файлы:** `22-web-frameworks.md` + `22-web-frameworks-practice.md`

**Цель:** Создавать веб-приложения.

**Темы:**
- `axum` — современный async фреймворк
- Роутинг
- Middleware
- Extractors (Path, Query, Json)
- State management
- Error handling в веб-контексте
- Validation: `validator`
- Templates: `askama` или `tera`

**Практика:**
- REST API для todo-list
- API с аутентификацией (JWT)
- CRUD приложение с базой данных
- HTML сервер с шаблонами

---

### Блок 23: Unsafe Rust
**Файлы:** `23-unsafe.md` + `23-unsafe-practice.md`

**Цель:** Понять, когда и как использовать unsafe.

**Темы:**
- `unsafe` блоки и функции
- Сырые указатели: `*const T`, `*mut T`
- Разыменование сырых указателей
- Вызов unsafe функций
- FFI (Foreign Function Interface)
- Связывание с C библиотеками
- Правила безопасности в unsafe
- `bindgen` для генерации FFI

**Практика:**
- Обёртка над C библиотекой
- Оптимизация критичного кода
- Реализация низкоуровневой структуры данных
- Интеграция с системными API

---

### Блок 24: Производительность и оптимизация
**Файлы:** `24-performance.md` + `24-performance-practice.md`

**Цель:** Писать быстрый код и профилировать.

**Темы:**
- Benchmarking: `criterion`
- Профилирование: `perf`, `flamegraph`
- Оптимизация аллокаций
- Zero-copy техники
- SIMD (кратко)
- Inline оптимизации
- LTO (Link Time Optimization)
- Memory layout и padding
- Cargo профили

**Практика:**
- Бенчмарки для алгоритмов
- Оптимизация обработки больших данных
- Профилирование и устранение bottlenecks
- Сравнение разных подходов

---

### Блок 25: Финальный проект
**Файлы:** `25-final-project.md`

**Цель:** Собрать всё воедино в реальном приложении.

**Проект:** Полнофункциональное приложение

Варианты:
1. **REST API сервис** с аутентификацией, базой данных, тестами
2. **CLI инструмент** (git-like, docker-like)
3. **Веб-скрапер** с concurrency и сохранением в БД
4. **Чат-сервер** с WebSockets и Redis
5. **Игровой сервер** с state management
6. **Компилятор/интерпретатор** для простого языка

**Требования:**
- Модульная структура
- Тесты (unit + integration)
- Обработка ошибок
- Async где необходимо
- Документация
- CI/CD (GitHub Actions)
- Dockerized deployment (опционально)

---

## Дополнительные блоки (опционально, для продвинутых)

### Блок 26: WebAssembly
**Файлы:** `26-wasm.md`

**Темы:**
- `wasm-pack`
- Интеграция Rust + JavaScript
- DOM манипуляция из Rust
- Производительность WASM vs JS

### Блок 27: Embedded системы
**Файлы:** `27-embedded.md`

**Темы:**
- `no_std` программирование
- Embedded HAL
- Работа с железом
- Real-time операционки

### Блок 28: Game Development
**Файлы:** `28-gamedev.md`

**Темы:**
- `bevy` engine
- ECS (Entity Component System)
- Графика и физика
- Input handling

### Блок 29: Крипто и блокчейн
**Файлы:** `29-crypto.md`

**Темы:**
- Криптографические примитивы
- Хеширование
- Подписи
- Substrate framework (кратко)

### Блок 30: Продвинутые паттерны
**Файлы:** `30-advanced-patterns.md`

**Темы:**
- Newtype pattern
- Type state pattern
- Builder pattern
- Phantom types
- GATs (Generic Associated Types)

---

## Рекомендуемый порядок изучения

**Путь для начинающих (линейный):**
1-5 (Основы) → 6-7 (Коллекции и ошибки) → 8-9 (Generics и Traits) → 10 (Тестирование) → 11-12 (Итераторы и умные указатели) → 14-15 (Модули и Cargo) → 17 (Файлы) → 20 (CLI) → 25 (Проект)

**Путь для практиков (проектно-ориентированный):**
- Блоки 1-5 → мини-проект (CLI калькулятор)
- Блоки 6-10 → проект (Поисковик файлов)
- Блоки 11-15 → рефакторинг в библиотеку
- Блоки 17-19 → проект (HTTP клиент/сервер)
- Блоки 20-22 → REST API
- Блок 25 → итоговый проект

**Путь для веб-разработчиков:**
1-7 (Основы) → 8-9 (Generics/Traits) → 14-15 (Модули) → 18-19 (Async/HTTP) → 21-22 (БД и веб-фреймворки) → проект API

---

## Структура каждого урока (шаблон)

Каждый урок должен включать:

1. **Введение** — зачем это нужно, где применяется
2. **Концепция** — теория с диаграммами памяти
3. **Синтаксис** — примеры кода с объяснениями
4. **Сравнения** — с другими языками (C++, JavaScript)
5. **Типичные ошибки** — что не компилируется и почему
6. **Best Practices** — идиоматичный Rust
7. **Задания** — 3-5 упражнений
8. **Итоги** — краткая выжимка
9. **Что дальше?** — связь со следующим блоком
10. **Ресурсы** — The Book, Rust by Example, статьи

Каждый урок должен сопровождаться **практическими файлами** с полным кодом проектов.

---

## Метрики прогресса

После каждого блока студент должен уметь:
- ✅ Объяснить концепцию и связь с ownership/borrowing
- ✅ Написать код, который компилируется с первого раза
- ✅ Прочитать и понять сообщения компилятора
- ✅ Исправить типичные ошибки borrow checker
- ✅ Применить знания в мини-проекте

---

## Приоритет для следующих уроков

**Срочные (база для любого проекта):**
1. Блок 1: Основы синтаксиса
2. Блок 2: Функции и управление потоком
3. Блок 3: Ownership и borrowing (КРИТИЧНО)
4. Блок 4: Структуры
5. Блок 5: Enums и pattern matching

**Важные (для реальных приложений):**
6. Блок 6: Коллекции
7. Блок 7: Обработка ошибок
8. Блок 9: Трейты
9. Блок 10: Тестирование
10. Блок 14: Модули

**Полезные (расширяют возможности):**
11. Блок 11: Итераторы и замыкания
12. Блок 17: Файловый ввод-вывод
13. Блок 20: CLI приложения

**Для веб-разработки:**
14. Блок 18: Сети и HTTP
15. Блок 19: Async/Await
16. Блок 21: Базы данных
17. Блок 22: Веб-фреймворки

---

## Ключевые отличия от других языков

**Для тех, кто знает JavaScript:**
- Нет garbage collector — владение и borrowing
- Строгая типизация во время компиляции
- Нет null — есть Option<T>
- Нет exceptions — есть Result<T, E>
- Иммутабельность по умолчанию

**Для тех, кто знает Python:**
- Компилируемый язык (не интерпретируемый)
- Явные типы (хотя есть type inference)
- Управление памятью без GC
- Производительность как у C/C++

**Для тех, кто знает C++:**
- Безопасность памяти по умолчанию
- Нет manual memory management (new/delete)
- Современный синтаксис
- Мощная система типов
- Cargo вместо make/cmake

---

## Философия Rust

1. **Безопасность** — предотвращение ошибок на этапе компиляции
2. **Скорость** — zero-cost abstractions
3. **Конкурентность** — fearless concurrency
4. **Эргономичность** — приятный developer experience

**Мантра:** _"If it compiles, it works"_ ✨

---

## Ресурсы для изучения

**Официальные:**
- [The Rust Programming Language](https://doc.rust-lang.org/book/) (The Book)
- [Rust by Example](https://doc.rust-lang.org/rust-by-example/)
- [Rustlings](https://github.com/rust-lang/rustlings) — интерактивные упражнения
- [Rust Standard Library Docs](https://doc.rust-lang.org/std/)

**Продвинутые:**
- [The Rustonomicon](https://doc.rust-lang.org/nomicon/) — unsafe Rust
- [Async Book](https://rust-lang.github.io/async-book/)
- [Rust Design Patterns](https://rust-unofficial.github.io/patterns/)

**Практика:**
- [Exercism Rust Track](https://exercism.org/tracks/rust)
- [Advent of Code](https://adventofcode.com/) на Rust
- [Project Euler](https://projecteuler.net/) на Rust

---

Этот роадмап покрывает всё необходимое для backend/системного программирования на Rust и готовит к реальным проектам в продакшене.

**🦀 Добро пожаловать в мир Rust!**
