# Практика — Блок 16: TypeScript + React

Задачи:
- Типизированный компонент с props (interface/type).
- Форма с типизацией event и state.
- Custom hook с generics и типизацией.
- Redux Toolkit + TypeScript — типизация store и thunks.
