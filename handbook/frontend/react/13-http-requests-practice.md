# Практика — Блок 13: HTTP requests

Задачи:
- Загрузка списка постов через fetch.
- CRUD приложение (POST/PUT/DELETE) против mock API (jsonplaceholder).
- Реализовать debounce-поиск через API.
- Pagination при загрузке большого списка.
