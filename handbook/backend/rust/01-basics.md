# Блок 1: Основы синтаксиса и первая программа

## Цель

Написать первую программу на Rust, понять базовый синтаксис, научиться работать с cargo и освоить основные концепции языка.

---

## 1. Установка Rust (rustup, cargo)

### Что такое Rust?

Rust — это системный язык программирования, который гарантирует безопасность памяти без сборщика мусора. Rust компилируется в машинный код и обеспечивает высокую производительность.

### Установка через rustup

**rustup** — это установщик и менеджер версий Rust.

**Для Linux/macOS:**
```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
```

**Для Windows:**
Скачайте установщик с [https://rustup.rs/](https://rustup.rs/)

После установки выполните:
```bash
rustup --version
cargo --version
rustc --version
```

### Что устанавливается?

- **rustc** — компилятор Rust
- **cargo** — менеджер пакетов и система сборки
- **rustup** — инструмент для управления версиями Rust

### Обновление Rust

```bash
rustup update
```

---

## 2. Cargo — менеджер пакетов и сборки

### Создание нового проекта

```bash
cargo new hello_world
cd hello_world
```

Структура проекта:
```
hello_world/
├── Cargo.toml       # Файл конфигурации проекта (манифест)
└── src/
    └── main.rs      # Исходный код
```

### Cargo.toml

```toml
[package]
name = "hello_world"
version = "0.1.0"
edition = "2021"

[dependencies]
# Зависимости будут добавляться сюда
```

### Основные команды cargo

| Команда | Описание |
|---------|----------|
| `cargo new <name>` | Создать новый проект |
| `cargo build` | Скомпилировать проект (debug режим) |
| `cargo build --release` | Скомпилировать с оптимизациями |
| `cargo run` | Скомпилировать и запустить |
| `cargo check` | Проверить код без компиляции (быстрее) |
| `cargo clean` | Удалить скомпилированные файлы |
| `cargo test` | Запустить тесты |

### Где находится скомпилированный файл?

- **Debug:** `target/debug/hello_world`
- **Release:** `target/release/hello_world`

---

## 3. Функция main()

Каждая исполняемая программа на Rust начинается с функции `main()`.

```rust
fn main() {
    // Код программы
}
```

**Синтаксис:**
- `fn` — ключевое слово для объявления функции
- `main` — имя функции (точка входа)
- `()` — параметры (пока пустые)
- `{}` — тело функции

### Первая программа

**src/main.rs:**
```rust
fn main() {
    println!("Hello, world!");
}
```

**Запуск:**
```bash
cargo run
```

**Вывод:**
```
   Compiling hello_world v0.1.0
    Finished dev [unoptimized + debuginfo] target(s) in 0.50s
     Running `target/debug/hello_world`
Hello, world!
```

---

## 4. Макрос println!

### Что такое макрос?

Макросы в Rust — это код, который пишет код. Отличаются от функций восклицательным знаком `!`.

```rust
println!("Hello, world!");  // Макрос
```

### Форматированный вывод

```rust
fn main() {
    // Простой вывод
    println!("Hello, world!");
    
    // С переменными
    let name = "Rust";
    println!("Hello, {}!", name);
    
    // Несколько значений
    let x = 5;
    let y = 10;
    println!("x = {}, y = {}", x, y);
    
    // Именованные параметры
    println!("{name} is {age} years old", name = "Rust", age = 10);
    
    // Форматирование чисел
    println!("Десятичное: {}", 255);
    println!("Бинарное: {:b}", 255);
    println!("Шестнадцатеричное: {:x}", 255);
    println!("С двумя знаками после запятой: {:.2}", 3.14159);
}
```

### Другие макросы вывода

```rust
print!("Без переноса строки");
println!("С переносом строки");
eprint!("Ошибка в stderr");
eprintln!("Ошибка в stderr с переносом");
```

---

## 5. Переменные: let и mut

### Неизменяемые переменные (по умолчанию)

В Rust переменные **неизменяемы** по умолчанию:

```rust
fn main() {
    let x = 5;
    println!("x = {}", x);
    
    // x = 10; // ОШИБКА! Нельзя изменить неизменяемую переменную
}
```

**Почему неизменяемость по умолчанию?**
- Безопасность
- Многопоточность (проще рассуждать о коде)
- Компилятор может лучше оптимизировать

### Изменяемые переменные (mut)

Чтобы переменная была изменяемой, используйте `mut`:

```rust
fn main() {
    let mut x = 5;
    println!("x = {}", x);
    
    x = 10; // Теперь можно изменить
    println!("x = {}", x);
}
```

### Правила именования

- Используйте `snake_case` для имён переменных
- Начинайте с буквы или подчёркивания
- Можно использовать буквы, цифры, подчёркивания

```rust
let user_age = 25;
let _private_var = 42;
let my_var_123 = "test";
```

### Константы

Константы всегда неизменяемы и должны иметь явный тип:

```rust
const MAX_POINTS: u32 = 100_000;
const PI: f64 = 3.14159265359;

fn main() {
    println!("MAX_POINTS = {}", MAX_POINTS);
}
```

**Отличия от let:**
- Используют `UPPER_CASE`
- Должен быть указан тип
- Можно объявлять в глобальной области
- Значение должно быть известно на этапе компиляции

---

## 6. Примитивные типы

### Целые числа (integers)

| Тип | Размер | Диапазон |
|-----|--------|----------|
| `i8` | 8 бит | -128 до 127 |
| `i16` | 16 бит | -32,768 до 32,767 |
| `i32` | 32 бита | -2,147,483,648 до 2,147,483,647 |
| `i64` | 64 бита | -(2^63) до 2^63-1 |
| `i128` | 128 бит | Очень большой диапазон |
| `isize` | Зависит от архитектуры | 32 или 64 бита |

| Тип | Размер | Диапазон |
|-----|--------|----------|
| `u8` | 8 бит | 0 до 255 |
| `u16` | 16 бит | 0 до 65,535 |
| `u32` | 32 бита | 0 до 4,294,967,295 |
| `u64` | 64 бита | 0 до 2^64-1 |
| `u128` | 128 бит | Очень большое число |
| `usize` | Зависит от архитектуры | 32 или 64 бита |

**Примеры:**

```rust
fn main() {
    let x: i32 = 42;          // Знаковое 32-битное
    let y: u32 = 42;          // Беззнаковое 32-битное
    let z = 42;               // По умолчанию i32
    
    // Литералы с суффиксом типа
    let a = 42u8;             // u8
    let b = 42i64;            // i64
    
    // Разделители для читаемости
    let million = 1_000_000;
    
    // Разные системы счисления
    let hex = 0xff;           // Шестнадцатеричное
    let octal = 0o77;         // Восьмеричное
    let binary = 0b1111_0000; // Двоичное
    let byte = b'A';          // Байт (только u8)
}
```

### Числа с плавающей точкой (floating-point)

```rust
fn main() {
    let x: f32 = 3.14;        // 32-битное
    let y: f64 = 2.71828;     // 64-битное (по умолчанию)
    let z = 2.0;              // По умолчанию f64
}
```

### Булевы значения (bool)

```rust
fn main() {
    let is_active: bool = true;
    let is_done = false;
    
    println!("is_active = {}", is_active);
}
```

### Символы (char)

```rust
fn main() {
    let letter: char = 'A';
    let emoji: char = '😀';
    let cyrillic: char = 'Я';
    
    println!("letter = {}", letter);
    println!("emoji = {}", emoji);
}
```

**Важно:**
- `char` — это Unicode Scalar Value (4 байта)
- Используйте одинарные кавычки `'`
- Строки используют двойные кавычки `"`

### Автоматический вывод типов

Rust умеет выводить типы автоматически:

```rust
fn main() {
    let x = 5;           // i32 (по умолчанию)
    let y = 2.0;         // f64 (по умолчанию)
    let flag = true;     // bool
    let letter = 'z';    // char
}
```

### Явное указание типов

```rust
fn main() {
    let x: i32 = 5;
    let y: f64 = 2.0;
    let flag: bool = true;
}
```

---

## 7. Комментарии

### Однострочные комментарии

```rust
// Это однострочный комментарий
fn main() {
    let x = 5; // Комментарий после кода
}
```

### Многострочные комментарии

```rust
/*
Это многострочный
комментарий
*/
fn main() {
    let x = /* встроенный комментарий */ 5;
}
```

### Документационные комментарии

Для документирования функций, структур и модулей:

```rust
/// Эта функция приветствует пользователя.
/// 
/// # Примеры
/// 
/// ```
/// greet("Rust");
/// ```
fn greet(name: &str) {
    println!("Hello, {}!", name);
}

//! Документация для модуля или крейта
//! Используется в начале файла
```

**Генерация документации:**
```bash
cargo doc --open
```

---

## 8. Shadowing (затенение переменных)

Shadowing позволяет переобъявить переменную с тем же именем:

```rust
fn main() {
    let x = 5;
    println!("x = {}", x); // 5
    
    let x = x + 1;
    println!("x = {}", x); // 6
    
    let x = x * 2;
    println!("x = {}", x); // 12
}
```

### Отличие от mut

```rust
fn main() {
    // Shadowing — можно изменить тип
    let spaces = "   ";
    let spaces = spaces.len(); // Теперь число
    
    // mut — нельзя изменить тип
    let mut spaces2 = "   ";
    // spaces2 = spaces2.len(); // ОШИБКА!
}
```

### Область видимости (scope)

```rust
fn main() {
    let x = 5;
    
    {
        let x = x * 2; // Новая переменная в новой области
        println!("Внутри блока x = {}", x); // 10
    }
    
    println!("Снаружи блока x = {}", x); // 5
}
```

---

## 9. Арифметические операции

```rust
fn main() {
    // Основные операции
    let sum = 5 + 10;
    let difference = 95 - 45;
    let product = 4 * 30;
    let quotient = 56 / 32;        // Целочисленное деление: 1
    let quotient_float = 56.0 / 32.0; // 1.75
    let remainder = 43 % 5;        // Остаток от деления: 3
    
    println!("sum = {}", sum);
    println!("difference = {}", difference);
    println!("product = {}", product);
    println!("quotient = {}", quotient);
    println!("quotient_float = {}", quotient_float);
    println!("remainder = {}", remainder);
}
```

### Составные операторы

```rust
fn main() {
    let mut x = 5;
    x += 10;  // x = x + 10
    x -= 3;   // x = x - 3
    x *= 2;   // x = x * 2
    x /= 4;   // x = x / 4
    x %= 3;   // x = x % 3
    
    println!("x = {}", x);
}
```

---

## 10. Приведение типов (type casting)

```rust
fn main() {
    let x: i32 = 42;
    let y: f64 = x as f64; // Явное приведение типа
    
    println!("x = {}, y = {}", x, y);
    
    // Приведение между числовыми типами
    let a: u8 = 255;
    let b: u32 = a as u32;
    
    // Будьте осторожны с переполнением!
    let c: i32 = 1000;
    let d: u8 = c as u8; // Обрезается до 232 (1000 % 256)
    println!("d = {}", d);
}
```

---

## Резюме Блока 1

### Что мы изучили:

✅ Установка Rust через `rustup`  
✅ Работа с `cargo` (new, build, run)  
✅ Функция `main()` — точка входа  
✅ Макрос `println!` для вывода  
✅ Переменные: `let` (неизменяемые) и `mut` (изменяемые)  
✅ Примитивные типы: целые числа, float, bool, char  
✅ Комментарии: `//`, `/* */`, `///`  
✅ Shadowing переменных  
✅ Базовые арифметические операции  
✅ Приведение типов с `as`  

### Следующие шаги:

Переходите к `01-basics-practice.md` для выполнения практических заданий!

---

## Полезные ресурсы

- [The Rust Programming Language (книга)](https://doc.rust-lang.org/book/)
- [Rust by Example](https://doc.rust-lang.org/rust-by-example/)
- [Rustlings — интерактивные упражнения](https://github.com/rust-lang/rustlings)
- [Документация Rust](https://doc.rust-lang.org/)
