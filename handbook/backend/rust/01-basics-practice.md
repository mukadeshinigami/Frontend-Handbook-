# Блок 1: Практика — Основы синтаксиса

## Цель практики

Закрепить знания из `01-basics.md` через выполнение практических заданий: написать Hello World, создать калькулятор и конвертер температуры.

---

## Задание 1: Hello World с cargo

### Описание

Создайте первый проект на Rust с помощью cargo и запустите программу "Hello, World!".

### Шаги выполнения

1. Откройте терминал
2. Создайте новый проект:
   ```bash
   cargo new hello_world
   cd hello_world
   ```

3. Откройте файл `src/main.rs` — он уже содержит Hello World:
   ```rust
   fn main() {
       println!("Hello, world!");
   }
   ```

4. Запустите программу:
   ```bash
   cargo run
   ```

### Ожидаемый результат

```
   Compiling hello_world v0.1.0
    Finished dev [unoptimized + debuginfo] target(s) in 0.50s
     Running `target/debug/hello_world`
Hello, world!
```

### Модификация задания

Измените программу так, чтобы она выводила:
- Ваше имя
- Текущую дату
- Приветственное сообщение с использованием переменных

**Пример решения:**

```rust
fn main() {
    let name = "Алексей";
    let year = 2025;
    
    println!("Привет, {}!", name);
    println!("Добро пожаловать в мир Rust!");
    println!("Год: {}", year);
}
```

**Результат:**
```
Привет, Алексей!
Добро пожаловать в мир Rust!
Год: 2025
```

---

## Задание 2: Калькулятор простых операций

### Описание

Создайте калькулятор, который выполняет базовые арифметические операции (сложение, вычитание, умножение, деление, остаток от деления) с двумя числами.

### Требования

- Использовать переменные для хранения двух чисел
- Выполнить все 5 операций
- Вывести результаты каждой операции
- Использовать форматированный вывод

### Решение

Создайте новый проект:
```bash
cargo new calculator
cd calculator
```

**src/main.rs:**

```rust
fn main() {
    // Определяем два числа
    let a = 20;
    let b = 6;
    
    // Выполняем операции
    let sum = a + b;
    let difference = a - b;
    let product = a * b;
    let quotient = a / b;        // Целочисленное деление
    let remainder = a % b;       // Остаток от деления
    
    // Выводим результаты
    println!("=== Калькулятор ===");
    println!("Числа: a = {}, b = {}", a, b);
    println!();
    println!("Сложение: {} + {} = {}", a, b, sum);
    println!("Вычитание: {} - {} = {}", a, b, difference);
    println!("Умножение: {} * {} = {}", a, b, product);
    println!("Деление: {} / {} = {}", a, b, quotient);
    println!("Остаток: {} % {} = {}", a, b, remainder);
}
```

**Вывод:**
```
=== Калькулятор ===
Числа: a = 20, b = 6

Сложение: 20 + 6 = 26
Вычитание: 20 - 6 = 14
Умножение: 20 * 6 = 120
Деление: 20 / 6 = 3
Остаток: 20 % 6 = 2
```

### Улучшенная версия (с вещественными числами)

```rust
fn main() {
    // Используем f64 для работы с дробными числами
    let a: f64 = 20.0;
    let b: f64 = 6.0;
    
    let sum = a + b;
    let difference = a - b;
    let product = a * b;
    let quotient = a / b;        // Теперь дробное деление
    
    println!("=== Калькулятор (float) ===");
    println!("Числа: a = {}, b = {}", a, b);
    println!();
    println!("Сложение: {} + {} = {}", a, b, sum);
    println!("Вычитание: {} - {} = {}", a, b, difference);
    println!("Умножение: {} * {} = {}", a, b, product);
    println!("Деление: {} / {} = {:.2}", a, b, quotient); // Округление до 2 знаков
}
```

**Вывод:**
```
=== Калькулятор (float) ===
Числа: a = 20, b = 6

Сложение: 20 + 6 = 26
Вычитание: 20 - 6 = 14
Умножение: 20 * 6 = 120
Деление: 20 / 6 = 3.33
```

### Дополнительное задание

Расширьте калькулятор:
- Добавьте возведение в степень (используйте `a.powf(2.0)` для `f64`)
- Добавьте извлечение квадратного корня (используйте `a.sqrt()`)
- Вычислите среднее арифметическое двух чисел

**Решение:**

```rust
fn main() {
    let a: f64 = 16.0;
    let b: f64 = 4.0;
    
    let sum = a + b;
    let difference = a - b;
    let product = a * b;
    let quotient = a / b;
    let power = a.powf(2.0);      // Возведение в квадрат
    let sqrt = a.sqrt();          // Квадратный корень
    let average = (a + b) / 2.0;  // Среднее арифметическое
    
    println!("=== Расширенный калькулятор ===");
    println!("Числа: a = {}, b = {}", a, b);
    println!();
    println!("Сложение: {}", sum);
    println!("Вычитание: {}", difference);
    println!("Умножение: {}", product);
    println!("Деление: {}", quotient);
    println!("a в квадрате: {}", power);
    println!("Квадратный корень из a: {}", sqrt);
    println!("Среднее арифметическое: {}", average);
}
```

---

## Задание 3: Конвертер температуры (Цельсий ↔ Фаренгейт)

### Описание

Создайте программу, которая конвертирует температуру из градусов Цельсия в Фаренгейты и обратно.

### Формулы

- **Цельсий → Фаренгейт:** `F = C × 9/5 + 32`
- **Фаренгейт → Цельсий:** `C = (F - 32) × 5/9`

### Требования

- Использовать переменные для хранения температур
- Выполнить оба преобразования
- Вывести результаты с округлением до 2 знаков после запятой

### Решение

Создайте новый проект:
```bash
cargo new temperature_converter
cd temperature_converter
```

**src/main.rs:**

```rust
fn main() {
    // Температура в градусах Цельсия
    let celsius: f64 = 25.0;
    
    // Конвертация: Цельсий → Фаренгейт
    let fahrenheit = celsius * 9.0 / 5.0 + 32.0;
    
    println!("=== Конвертер температуры ===");
    println!("{:.2}°C = {:.2}°F", celsius, fahrenheit);
    
    // Обратная конвертация: Фаренгейт → Цельсий
    let fahrenheit2: f64 = 77.0;
    let celsius2 = (fahrenheit2 - 32.0) * 5.0 / 9.0;
    
    println!("{:.2}°F = {:.2}°C", fahrenheit2, celsius2);
}
```

**Вывод:**
```
=== Конвертер температуры ===
25.00°C = 77.00°F
77.00°F = 25.00°C
```

### Улучшенная версия (с несколькими значениями)

```rust
fn main() {
    println!("=== Конвертер температуры ===\n");
    
    // Массив значений для конвертации
    let temps_celsius = [0.0, 25.0, 37.0, 100.0];
    
    println!("Цельсий → Фаренгейт:");
    for temp in temps_celsius {
        let fahrenheit = temp * 9.0 / 5.0 + 32.0;
        println!("{:>6.1}°C = {:>6.1}°F", temp, fahrenheit);
    }
    
    println!("\nФаренгейт → Цельсий:");
    let temps_fahrenheit = [32.0, 77.0, 98.6, 212.0];
    
    for temp in temps_fahrenheit {
        let celsius = (temp - 32.0) * 5.0 / 9.0;
        println!("{:>6.1}°F = {:>6.1}°C", temp, celsius);
    }
}
```

**Вывод:**
```
=== Конвертер температуры ===

Цельсий → Фаренгейт:
   0.0°C =   32.0°F
  25.0°C =   77.0°F
  37.0°C =   98.6°F
 100.0°C =  212.0°F

Фаренгейт → Цельсий:
  32.0°F =    0.0°C
  77.0°F =   25.0°C
  98.6°F =   37.0°C
 212.0°F =  100.0°C
```

### Версия с переменными и пояснениями

```rust
fn main() {
    println!("=== Конвертер температуры ===\n");
    
    // Температура кипения воды
    let boiling_celsius = 100.0;
    let boiling_fahrenheit = boiling_celsius * 9.0 / 5.0 + 32.0;
    println!("Кипение воды:");
    println!("  {} °C = {} °F", boiling_celsius, boiling_fahrenheit);
    
    // Температура замерзания воды
    let freezing_celsius = 0.0;
    let freezing_fahrenheit = freezing_celsius * 9.0 / 5.0 + 32.0;
    println!("\nЗамерзание воды:");
    println!("  {} °C = {} °F", freezing_celsius, freezing_fahrenheit);
    
    // Нормальная температура тела
    let body_celsius = 36.6;
    let body_fahrenheit = body_celsius * 9.0 / 5.0 + 32.0;
    println!("\nНормальная температура тела:");
    println!("  {:.1} °C = {:.1} °F", body_celsius, body_fahrenheit);
    
    // Комнатная температура
    let room_celsius = 22.0;
    let room_fahrenheit = room_celsius * 9.0 / 5.0 + 32.0;
    println!("\nКомнатная температура:");
    println!("  {} °C = {:.1} °F", room_celsius, room_fahrenheit);
}
```

**Вывод:**
```
=== Конвертер температуры ===

Кипение воды:
  100 °C = 212 °F

Замерзание воды:
  0 °C = 32 °F

Нормальная температура тела:
  36.6 °C = 97.9 °F

Комнатная температура:
  22 °C = 71.6 °F
```

---

## Дополнительные упражнения

### Упражнение 4: Конвертер валют

Создайте программу для конвертации валют (например, USD → EUR, RUB → USD).

**Подсказка:**

```rust
fn main() {
    let usd = 100.0;
    let usd_to_eur_rate = 0.92;  // Курс: 1 USD = 0.92 EUR
    let eur = usd * usd_to_eur_rate;
    
    println!("{} USD = {:.2} EUR", usd, eur);
}
```

### Упражнение 5: Калькулятор ИМТ (индекс массы тела)

Формула: `ИМТ = вес (кг) / (рост (м))²`

```rust
fn main() {
    let weight_kg: f64 = 70.0;
    let height_m: f64 = 1.75;
    
    let bmi = weight_kg / (height_m * height_m);
    
    println!("Вес: {} кг", weight_kg);
    println!("Рост: {} м", height_m);
    println!("ИМТ: {:.2}", bmi);
}
```

### Упражнение 6: Работа с shadowing

Напишите программу, которая демонстрирует shadowing:

```rust
fn main() {
    let x = 5;
    println!("x = {}", x);
    
    let x = x + 1;
    println!("x = {}", x);
    
    {
        let x = x * 2;
        println!("Внутри блока x = {}", x);
    }
    
    println!("Снаружи блока x = {}", x);
    
    // Изменение типа через shadowing
    let spaces = "   ";
    println!("spaces (строка): '{}'", spaces);
    
    let spaces = spaces.len();
    println!("spaces (число): {}", spaces);
}
```

### Упражнение 7: Форматирование чисел

Поэкспериментируйте с форматированием вывода:

```rust
fn main() {
    let pi = 3.14159265359;
    
    println!("Число π:");
    println!("  По умолчанию: {}", pi);
    println!("  1 знак: {:.1}", pi);
    println!("  2 знака: {:.2}", pi);
    println!("  5 знаков: {:.5}", pi);
    
    let number = 255;
    println!("\nЧисло 255 в разных системах:");
    println!("  Десятичное: {}", number);
    println!("  Двоичное: {:b}", number);
    println!("  Восьмеричное: {:o}", number);
    println!("  Шестнадцатеричное: {:x}", number);
    println!("  Шестнадцатеричное (верхний регистр): {:X}", number);
}
```

---

## Проверка понимания

После выполнения всех заданий, убедитесь, что вы понимаете:

- ✅ Как создавать и запускать проекты через `cargo`
- ✅ Как объявлять переменные с `let` и `mut`
- ✅ Как использовать примитивные типы: `i32`, `u32`, `f64`, `bool`, `char`
- ✅ Как форматировать вывод с помощью `println!`
- ✅ Как выполнять арифметические операции
- ✅ Как работает shadowing переменных
- ✅ Разницу между целочисленным и вещественным делением
- ✅ Как приводить типы с помощью `as`

---

## Частые ошибки

### Ошибка 1: Попытка изменить неизменяемую переменную

```rust
fn main() {
    let x = 5;
    x = 10; // ОШИБКА: cannot assign twice to immutable variable
}
```

**Решение:** Используйте `mut`

```rust
fn main() {
    let mut x = 5;
    x = 10; // ОК
}
```

### Ошибка 2: Целочисленное деление вместо вещественного

```rust
fn main() {
    let result = 5 / 2; // Результат: 2 (не 2.5!)
    println!("{}", result);
}
```

**Решение:** Используйте `f64`

```rust
fn main() {
    let result = 5.0 / 2.0; // Результат: 2.5
    println!("{}", result);
}
```

### Ошибка 3: Неправильный тип при операции

```rust
fn main() {
    let x: i32 = 5;
    let y: f64 = 2.0;
    let result = x + y; // ОШИБКА: mismatched types
}
```

**Решение:** Приведите типы

```rust
fn main() {
    let x: i32 = 5;
    let y: f64 = 2.0;
    let result = x as f64 + y; // ОК
    println!("{}", result);
}
```

---

## Следующие шаги

После успешного выполнения всех заданий переходите к **Блоку 2: Функции и управление потоком** (`02-functions-control-flow.md`).

Там вы научитесь:
- Создавать собственные функции
- Использовать условия `if/else`
- Работать с циклами `loop`, `while`, `for`
- Применять `match` для сопоставления с образцом

---

## Полезные команды для повторения

```bash
# Создать новый проект
cargo new project_name

# Перейти в папку проекта
cd project_name

# Запустить проект
cargo run

# Проверить код без компиляции (быстрее)
cargo check

# Скомпилировать с оптимизациями
cargo build --release

# Очистить скомпилированные файлы
cargo clean
```

Успехов в изучении Rust! 🦀
