#include <stdio.h>
#include <stdbool.h>

int main(void) {

    int age = 25;
    float height = 1.80;
    char asd = 'a';
    bool is_active = true;
    bool is_done = false;

    printf("Height: %f\n", height);
    printf("Asd: %c\n", asd);
    printf("Is active: %d\n", is_active);
    printf("Is done: %d\n", is_done);
    
    return 0;
}
